import { styled } from "styled-components";

export const FormInputWrapper = styled.div`
  margin-top: 20px;
  .form-input .ant-collapse-header {
    align-items: center !important;
    flex-wrap: wrap !important;
  }
`;
